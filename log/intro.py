"""
Python的logging模块是Python标准库中用于记录日志的模块，它提供了灵活的日志记录系统，可以用来记录应用程序运行时的各种信息。
    使用logging模块，我们可以将日志输出到控制台、文件、邮件、网络服务器等不同的地方，并且可以自定义日志格式、日志级别等。

日志级别（从低到高）：
    DEBUG：用于调试的详细信息，通常在诊断问题时使用。
    INFO：确认程序按预期运行。
    WARNING：表示一些意外的事情发生，或者即将出现问题（比如磁盘空间不足）。但程序仍然继续运行。
    ERROR：由于更严重的问题，程序的一些功能已经不能正常执行。
    CRITICAL：严重的错误，表明程序本身可能无法继续运行。
我们可以设置一个日志级别，所有低于这个级别的日志消息将被忽略（不处理）。例如，如果设置为INFO，那么DEBUG级别的日志将被忽略，而INFO及更高级别的日志将会被处理。

logging模块的主要组件：
    Logger：应用程序代码直接使用的接口，用于生成日志。
    Handler：将日志记录发送到指定的目的地（控制台、文件等）。
    Filter：提供了更细粒度的控制，决定哪些日志记录将会被输出。
    Formatter：指定日志记录的最终输出格式。

基本使用步骤：
    创建一个logger。
    设置logger的日志级别。
    创建handler，并设置handler的日志级别。
    设置日志格式（formatter）并添加到handler。
    将handler添加到logger。
    但是，我们也可以使用简单的logging模块级别的函数（如logging.basicConfig()）来快速配置日志系统。

现在，我们来写一个案例，演示如何使用logging模块。
案例要求：
    创建一个logger，命名为'sample_logger'
    设置日志级别为DEBUG
    将日志同时输出到控制台和文件
    控制台只显示INFO及以上级别的日志，日志格式为：时间 - 日志级别 - 消息
    文件记录所有DEBUG及以上级别的日志，日志格式为：时间 - 日志级别 - 文件名 - 行号 - 消息
    我们将分别添加两个handler：一个StreamHandler（用于控制台）和一个FileHandler（用于文件）。
"""
